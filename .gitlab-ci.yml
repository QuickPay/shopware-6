image: docker:latest

stages:
  - syntax

before_script:
  - source ci/prepare

codesniffer:
  stage: syntax
  image: registry.services.wexo.dk:4567/wexo-services/docker-magento:shopware6-codesniffer-7.4
  script:
    - ~/phpcs.sh --ignore=*/dist/*
  allow_failure: false
